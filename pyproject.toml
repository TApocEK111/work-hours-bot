[project]
name = "work-hours-bot"
version = "0.0.1"
description = "Bot used to track working hours."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiogram>=3.26.0",
    "aiosqlite>=0.22.1",
]
